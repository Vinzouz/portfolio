export const routing = {
  // A list of all locales that are supported
  locales: ['fr', 'en'],

  // Locale par défaut : français
  defaultLocale: 'fr',

  // Strategy "never" : aucune locale dans l'URL, stockée dans un cookie
  localePrefix: 'never'
};

